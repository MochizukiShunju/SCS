<% if item.bookmarked_by?(current_user) %>
  <p><%= link_to "ブックマークを外す", item_bookmarks_path(item.id), method: :delete, remote: true  %></p>
  <% else %>
   <p><%= link_to "ブックマーク", item_bookmarks_path(item.id), method: :post, remote: true  %></p>
<% end %>