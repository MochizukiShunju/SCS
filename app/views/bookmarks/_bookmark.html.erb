<% if item.bookmarked_by?(current_user) %>
  <p><%= link_to "", item_bookmarks_path(item.id), method: :delete, remote: true, class: "fas fa-bookmark" %></p>
  <% else %>
   <p><%= link_to "", item_bookmarks_path(item.id), method: :post, remote: true, class: "far fa-bookmark"  %></p>
<% end %>