
<% if @notifications.exists? %>
  <% @notifications.each do | notification | %>
    <%= render 'notifications/notifications', {notification: notification} %>
  <% end %>
  <%= paginate @notifications %>
<% else %>
  <p>通知はありません</p>
<% end %>